let greetings: string;

greetings = "Hello World!";
greetings.toLowerCase();
console.log(greetings); // Hello World!

// number

let userId = 3462499;

userId.valueOf();


export {}; // This line is added to avoid the error: "Cannot redeclare block-scoped variable 'greetings'."